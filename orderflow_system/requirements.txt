duckdb>=1.5.0
pandas>=2.0.0
numpy>=1.24.0
pydantic>=2.0.0
pyyaml>=6.0
google-genai>=0.1.0

# Add when you wire up real data sources on your own machine:
# ibapi                # for IBKR TWS API
# databento             # for Databento historical + live tick data
